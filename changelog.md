# 更新日志

## 4.2.0 · 2026-09-19

- 新增最近活动页：自动记录插件处理、模型请求、工具调用、诊断及进程变化，支持筛选、分页和历史启动批次。
- 趋势图支持点击采样点或放大后分析区间，展示服务内存构成、进程变化、采样间隔和同期活动；并发线索不冒充插件独占内存。
- 活动只保存功能元数据，不保存聊天内容或工具参数；独立后端持久化并限制队列、速率与记录条数。
- 增加明确确认后才开启的限时 Python 分配追踪，默认关闭，到时退出；不接管其他工具的追踪器。
- 恢复依赖名称与源码位置展示，补全手动依赖审计，不再沿用自动审计的整轮短时预算。
- 同步插件和独立后端界面；增加生命周期、隐私、容量与三主题桌面/手机回归测试。

## 4.1.3 · 2026-09-18

- 增加自动诊断快照：后端模式下默认每 15 分钟生成一次轻量依赖审计和引用图结果；对象普查仍需手动执行。
- 诊断快照保存到 AstrBot 插件 KV，并同步到独立后端；重载、重启或打开独立 WebUI 后仍可查看最近结果和测量时间。
- 插件列表新增“诊断快照”列，显示最近引用图估算与对象普查结果，并区分快照数据与启动增量。
- 自动引用图扫描限制为约 1 秒 CPU 工作预算和 100000 个对象，降低对消息处理的影响。

## 4.1.2 · 2026-09-18

- 新增 MemoryScope 品牌 Logo：根目录 `logo.png` 可被 AstrBot 插件页面直接识别。
- 同步保留 SVG 源稿与 512/128/64 像素 PNG 资产，便于插件列表、详情页和文档使用。

## 4.1.1 · 2026-09-18

- 重设计松纸、极夜、暮映三套主题：柔光渐变背景、半透明导航与浮层、分层文字和按指标区分的数值颜色。
- 主题选择改为带真实配色缩略预览的面板，修复暗色主题下原生下拉菜单白底浅字的问题；补齐其他下拉选项的前景与背景。
- 诊断工具分别使用墨绿/薄荷/杏金、陶朱/琥珀/玫瑰、鸢尾色系，趋势图加入渐隐填充并保留清晰的曲线和刻度。
- 支持键盘选择、Escape 关闭、焦点返回及窄屏布局；增加三主题对比度和偏好保存的浏览器回归检查。
- 本次为插件界面更新，独立采集后端接口和采样逻辑不变。

## 4.1.0 · 2026-09-18

- 重建统一 WebUI，独立页与内嵌页共用框架，提供砂白、深海、暮紫主题。
- 修复切页不取数以及插件清单依赖旧本地数据导致的空白页面。
- 增加趋势时间范围、指标切换、自适应刻度、悬停读数、拖动放大及键盘操作。
- 增加插件搜索、状态筛选、排序、分页、详情抽屉与历史启动批次选择。
- 明确区分未测量、过期、错误与本地模式；手动诊断保持按需运行。
- 趋势接口按时间窗口读取，压缩长范围并保留各指标峰谷和缺失点；启动报告支持省略趋势数据。
- 增加显式 POST 引用图扫描接口、共享中英文翻译和浏览器回归检查。
- 适配 AstrBot 沙盒页面，主题与语言通过插件 KV 保留，避免 Dashboard 主题覆盖；增加请求参数兼容回归测试。
- 精简 README，将配置、部署、测量和 WebUI 使用说明拆分为独立文档。

## 4.0.0 · 2026-09-18

- 增加独立服务器观察后端、可选早期启动探针和 A/B/A 对照实验。
- systemd 持久化部署，历史保存在独立状态目录。
- 连接后端时停用重复的本地持续扫描，保留手动诊断。
- 用普通语言说明配置开关的作用、开销与测量局限。
